a = imread('uk.jpg');
x_max = size(a,2)
y_max = size(a,1)
for i = 1:x_max
    for j = 1:y_max
        if (a(j,i,1)>100 & a(j,i,3)>100)
            a(j,i,1)=255;
            a(j,i,2)=0;
            a(j,i,3)=0;
        elseif 
            a(j,i,1:3)=
        end
    end
end

image(a);
